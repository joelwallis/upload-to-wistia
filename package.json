{
  "name": "upload-to-wistia",
  "private": true,
  "scripts": {
    "start": "gulp build",
    "build": "gulp build",
    "build:html": "gulp build:html",
    "build:js": "gulp build:js",
    "build:js:vendor": "gulp build:js:vendor",
    "build:js:app": "gulp build:js:app",
    "build:css": "gulp build:css",
    "watch": "gulp watch",
    "watch:html": "gulp watch:html",
    "watch:js": "gulp watch:js",
    "watch:css": "gulp watch:css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "angular": "^1.5.8",
    "blueimp-file-upload": "^9.12.5",
    "bootstrap": "^3.3.7",
    "jquery": "^3.1.0",
    "jquery-ui": "^1.12.0"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-cli": "^1.2.2",
    "gulp-concat": "^2.6.0",
    "gulp-pug": "^3.0.4"
  }
}
